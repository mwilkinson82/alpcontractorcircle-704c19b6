// Deployment: the shared webhook branch cannot import across function folders.
// Single source of truth lives in ../_shared/cpm-intensive-validation.ts.
export * from "../_shared/cpm-intensive-validation.ts";
